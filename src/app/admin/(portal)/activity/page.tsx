export default function AdminActivityPage() {
  return <div className="rounded-[1.75rem] border border-white/10 bg-[#111213] p-8 text-white/68">Activity log surfacing is already seeded and will be expanded into a full audit table in the database phase.</div>;
}
